FROM nginx:latest

WORKDIR /docker

ARG ARC_TYPE
ENV DOCKERIZE_VERSION v0.6.1

# RUN tar -C /docker/nginx/html -xvf /docker/nginx/html/train.tar
# RUN tar -C /usr/local/bin -xzvf ./package/dockerize-linux-$ARC_TYPE-$DOCKERIZE_VERSION.tar.gz \
#     && rm ./package/dockerize-linux-$ARC_TYPE-$DOCKERIZE_VERSION.tar.gz

EXPOSE 80
